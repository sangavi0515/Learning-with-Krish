Static block is called when class loader is loading the class.A static initialization block can appear anywhere in the class body and can have any number of static initialization blocks.
Empty block will be called when we are creating the object with new keyword.
constructors are run EACH time a new instance of a class is created.

static block always gets executed first in java.The empty block is executed whenever we insantiate our class

there for the output of the program is
100
Static block
10
Empty block
constructor